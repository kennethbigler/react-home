import grey from '@material-ui/core/colors/grey';

const styles = {
  box: {
    cursor: 'default',
    backgroundColor: grey[200],
    height: 500,
    marginBottom: -500,
    minWidth: 1,
  },
  label: {
    position: 'relative',
    right: 22,
  },
};

export default styles;
