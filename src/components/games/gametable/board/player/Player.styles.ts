const player: React.CSSProperties = {
  borderRadius: 5,
  display: 'inline-block',
  margin: 10,
  padding: 20,
};

const width: React.CSSProperties = { minWidth: '100px' };

export default {
  player,
  width,
};
