import type { TextFieldProps } from "@mui/material";

/** Standard-variant TextField props shared by the finances entry dialogs. */
const dialogTextFieldProps: TextFieldProps = {
  variant: "standard",
  fullWidth: true,
  margin: "dense",
};

export default dialogTextFieldProps;
